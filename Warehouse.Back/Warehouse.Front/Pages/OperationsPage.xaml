<Page x:Class="Warehouse.Front.Pages.OperationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="OperationsPage"
      Background="White"
      FontFamily="Segoe UI"
      FontSize="14">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Журнал операций" FontSize="22" FontWeight="Bold" Margin="0,0,0,12"/>

        <!-- Фильтр по SKU -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Фильтр по SKU:" VerticalAlignment="Center" Width="100"/>
            <TextBox x:Name="txtFilterSku" Width="220" Height="26" Margin="0,0,8,0"/>
            <Button Content="Применить" Width="100" Click="BtnFilter_Click" Margin="0,0,8,0"/>
            <Button Content="Сброс" Width="80" Click="BtnClearFilter_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="dgOperations" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="120"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Цена/ед." Binding="{Binding PricePerUnit}" Width="100"/>
                <DataGridTextColumn Header="Причина" Binding="{Binding Reason}" Width="*" />
                <DataGridTextColumn Header="Дата операции" Binding="{Binding OperationDate}" Width="150"/>
                <DataGridTextColumn Header="Дата записи" Binding="{Binding RecordDate}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Обновить" Width="110" Click="BtnRefresh_Click"/>
        </StackPanel>
    </Grid>
</Page>
